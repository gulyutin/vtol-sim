{
  "name": "vtol-sim",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "dev:demo": "PROFILE=demo vite",
    "test:demo": "PROFILE=demo vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.ui.json && tsc --noEmit -p desktop/tsconfig.json",
    "desktop:dev": "node desktop/dev.mjs",
    "desktop:build": "node desktop/build.mjs",
    "desktop:build:demo": "node desktop/build.mjs --demo",
    "desktop:icons": "node desktop/make-icons.mjs"
  },
  "dependencies": {
    "@types/leaflet": "^1.9.22",
    "leaflet": "^1.9.4",
    "mp4-muxer": "^5.2.2",
    "three": "^0.186.0"
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "electron": "^44.3.0",
    "electron-builder": "^26.15.3",
    "occt-import-js": "^0.0.23",
    "typescript": "^5.9.0",
    "vite": "^7.3.6",
    "vitest": "^3.2.0"
  }
}
